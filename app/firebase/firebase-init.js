/* ── FIREBASE INIT ───────────────────────────────────────────────────
   Inicializa o app Firebase (SDK "compat", via <script> CDN — sem bundler)
   e expõe as instâncias usadas pelo resto do app: fbAuth, fbDb, fbStorage.
   Depende de firebase-config.js e dos scripts CDN do Firebase carregados
   antes deste arquivo em index.html. Implementado no módulo "Firebase".
   ──────────────────────────────────────────────────────────────── */
